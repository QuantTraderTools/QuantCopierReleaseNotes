<!--
Write your release notes below. This file is automatically read by the GitHub Actions
pipeline when you bump the version in DEPLOYMENT.md and a new release is created.

Important formatting rules:
1. Do not remove the `## Features`, `## Improvements`, or `## Bug Fixes` headers unless they are empty.
2. Use standard bullet points (`-`) for each item.
-->

## Improvements
Removed irrelevant phrases like `Trade Idea` and other noisy content from `Signal` type messages
